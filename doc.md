# LED模拟器使用说明书

适用于v1.4.8 修订日期：20250908第一版

[TOC]

## 软件介绍

LED模拟器是一款用于在电脑上模拟公交车等交通工具、某些场所的LED信息屏的软件。具有操作简单、功能丰富、模拟效果逼真等特点，适合在电脑屏幕上显示各种信息。

## 软件的安装、更新和卸载

### 安装
大部分情况下，下载软件得到的是一个压缩包，只需将其解压到电脑上的特定文件夹即完成了安装。
少数情况下，LED模拟器会提供一个安装程序，下载了安装程序的用户根据安装程序的引导安装即可。

![安装程序](/doc_image/安装程序.png)
![安装压缩包](/doc_image/安装压缩包.png)

### 更新
获取新版软件的渠道有[百度网盘链接](https://pan.baidu.com/s/1cc6tLshC78V7APPlABaqig?pwd=xf7k)、[蓝奏云链接](https://wwnt.lanzout.com/b00uyegi2b)(密码：fzjc)，进入LED模拟器交流讨论群下载三个途径。
更新软件时，您可能需要注意保留软件安装位置resources文件夹下面的一些字体资源文件和设置文件。

### 卸载
如果是通过解压压缩包安装软件的，直接删除软件安装文件夹即可。如果是通过安装程序安装的软件，请前往控制面板或设置，或者软件安装位置，运行卸载程序。

## 软件界面介绍
LED模拟器软件界面分为标题栏、菜单栏、主界面、状态栏。其中，主界面包括线路列表、线路设置（布局管理器）、节目单、节目内容编辑、图标管理器。

![界面介绍](/doc_image/界面介绍.png)

## 软件的使用

### 第一节 编辑

#### 新建文件
在“文件”菜单中，各项目的含义如下：

![文件菜单](/doc_image/文件菜单.png)
- 新建BSU文件：新建一个LED模拟器路牌文件；
- 打开：打开一个LED模拟器路牌文件，与“线路列表”上方的“打开文件”按钮等效；
- 保存：保存当前正在编辑的内容为LED模拟器路牌文件，与“线路列表”上方的“保存”按钮等效，快捷键为Ctrl+S。；
- 另存为：将当前正在编辑的内容保存为一份新的LED模拟器路牌文件；
- 关闭：关闭文件，并关闭LED模拟器。

LED模拟器刚打开时，会**自动**新建一个LED模拟器文件，此时可直接往线路列表中添加线路。
若已经打开了一个LED模拟器路牌文件，这时要新建文件，应该点击“文件”菜单中的“新建BSU文件”。

#### 添加线路
要开始进行模拟，需要往线路列表内添加线路。

![线路列表1](/doc_image/线路列表1.png)

点击线路列表的“新增线路”，此时弹出新增线路对话框

![新增线路对话框](/doc_image/新增线路对话框.png)

在对话框内输入线路名称，选择预设、刷新率，然后选择需要的路牌，并设置路牌的颜色模式、分辨率和点阵类型。
若后期需要修改，可以在线路列表下方修改。修改方式参考“设置节目名称和持续时间”节。

LED模拟器支持3种路牌预设，分别是“自定义”、“北京公交”和“普通”。
- 自定义：可在布局管理器中自行设置路牌的布局；
- 北京公交：使用北京公交的布局预设；
- 普通：使用普通布局预设。

北京公交预设的布局和普通预设布局如图所示，区别是：北京公交预设的前后路牌在布局1-4的示意图的红区中使用(8,12)规格的点阵，而普通预设使用的是(6,6)规格的点阵。

![preset](/doc_image/preset.png)

#### 添加节目

**北京公交/普通**
在北京公交布局和普通布局中，用户需要在布局管理器中为当前路牌手动输入所选布局中，路牌的各个参数的数值（点数），然后点击“确认修改”，为每个路牌设置完参数后，再点击“添加到节目单>>”，即可向节目单中添加一个节目。
如图为添加一条北京公交线路，然后为前侧路牌设置布局和参数：使用布局5，wid1为48，然后点击确认修改：

![新建北京公交线路](/doc_image/新建北京公交线路.png)
![北京公交设置布局](/doc_image/北京公交设置布局.png)

设置完各个路牌的布局后，点击“添加到节目单”

**自定义**
在自定义布局中，用户需要通过点击布局管理器中的布局示意图为每个路牌完成布局，然后点击“添加到节目单>>”，即可向节目单中添加一个节目。

![自定义布局操作](/doc_image/自定义布局操作.png)

点击布局示意图后会弹出如图所示的对话框。用户可用选择“更改屏幕”、“水平布局”和“垂直布局”三个操作。
- 更改屏幕：通过修改点阵类型来改变当前点击区域的像素密度；
- 水平布局：将当前区域根据输入的宽度分成水平的两部分；
- 垂直布局：将当前区域根据输入的宽度分成垂直的两部分；

此外，布局管理器在自定义布局模式下支持单次编辑的步骤撤销、重现。在切换选中的路牌后会丢失之前的历史记录。要使布局回到初始状态，请持续点击“←”按键。

#### 设置节目名称和持续时间
往节目单中添加一个新节目后，节目名称和持续时间会沿用节目单中“名称”文本框和“持续”数值框中的值。如需修改，选定需要修改的节目后，在“名称”文本框或“持续”数值框中输入新的值，然后点击节目单或主界面的其他空白区域，或按小键盘的Enter键，即可完成修改。

![新节目](/doc_image/新节目.png)

第一次添加节目时，需要注意节目的持续时间可能是0，会影响节目的显示效果。

#### 查看各个路牌的布局
选定节目单中的一个节目后，布局管理器会呈现该节目选定路牌使用的布局，“节目内容编辑”中，“屏幕分区”会显示该节目选定路牌对应的分区。可在线路设置（布局管理器）切换路牌以中查看该节目各个路牌的布局，此操作不会与“节目内容编辑”中选定的路牌同步，而在“节目内容编辑”中切换路牌时，线路设置（布局管理器）中的路牌会同步切换。

![节目内容编辑](/doc_image/节目内容编辑.png)

#### 往路牌中填充内容
选中要编辑的节目后，在“节目内容编辑”中选择一个路牌，在“屏幕分区”列表中选中一个分区，即可在“节目内容编辑”右侧的数据编辑区编辑该分区显示的内容。包括字体、显示仿宋。文字、颜色、缩放等的调整。
完成内容编辑后，点击“确认”或者回车键，即可提交内容到选定分区。此时在“屏幕分区”列表中，右键点击可导出该分区的内容图像。

接下来详细介绍数据编辑区的使用方法：
- 主字体：目标语言的文字（除ASCII字符外）采用的字体。默认的字体列表中的字体可能不支持一些特殊语言的字符。支持ttc、ttf字体和HZK16、HZK24。
- 字号（主字体）：调整主字体的字号。使用HZK16、HZK24时，务必调整为16或24，否则可能不能正常显示字符。
- ASCII字体：ASCII字符使用的字体。默认为点阵字体，勾选“不使用点阵ASCII字体”后，将从主字体列表中取可用的字体。
- 字号（ASCII字体）：当ASCII字体为点阵字体时，此项不可用。仅当“不使用点阵ASCII字体”被勾选后才可用。
- 竖直偏移：微调字体取模时的竖直偏移量，用于字符下沉或上浮时的调整。
- 显示方式：提供内容的多种显示方式。
- 文字方向：可为内容的文本选择横向或竖向排列。
- 不使用点阵ASCII字体：不使用点阵ASCII字体。
- 竖向时旋转ASCII字体：竖向时旋转ASCII字体。
- 字间距：调整字间距。大于0时单位为点数，小于0时单位为百分比。
- 加粗（横竖）：调整支持的字体的横向、竖向粗细（支持的字体为主字体列表中列出的字体，即ttc、ttf、HZK）。
- 水平位置：调整显示内容的整体水平偏移位置。
- 竖直位置：调整显示内容的整体竖直偏移位置。
- 水平对齐：调整显示内容的水平对齐方式：-1：右对齐，0：居中，1：左对齐。
- 竖直对齐：调整显示内容的竖直对齐方式：-1：下对齐，0：居中，1：上对齐。
- 缩放比例（横竖）：调整显示内容的横向、竖向缩放比例。
- 自动缩放：自动确认水平方向的缩放比例。此时忽略输入的缩放比例。
- 仅缩放系统字体：调整支持的字体的横向、竖向缩放比例（支持的字体为主字体列表中列出的字体，即ttc、ttf、HZK），当此选项勾选时，自动缩放会失效。
- 文字：要显示的内容。支持utf-8编码的字符串。
- 单元颜色（单色牌选择）：线路为单色模式时，选择分区颜色。
- 单元颜色（彩色牌选择）：线路为彩色模式时，选择分区的默认颜色（未设置单字颜色时的字符颜色、某些显示方式中使用的颜色）。
- 颜色/多行：打开设置单字颜色和多行模式的对话框。用户可在此对话框编辑显示内容中的单个字符的前景色和背景色，同时可用设置是否以多行模式显示内容，以及调整行间距。**注意：勾选了“自动缩放”时，若在此对话框里勾选了“多行”，会使内容无法显示。若要多行显示，则应该取消勾选“自动缩放”，若要显示内容自动缩放，则应该不勾选“多行”**。
- 确认：提交以上内容。

#### 为显示内容添加图标
双击“图标管理器”中的图标，即可将图标插入到显示内容末尾。在显示内容中使用图标，请用反引号"`"将图标代号包围。

#### 路牌中节目的高级设置
点击“屏幕设置”，即可打开**当前节目**在**当前路牌**的设置界面，如图所示：

![路牌节目设置](/doc_image/路牌节目设置.png)

- 背景设置：可设置纯色背景和图片背景。
- 遮罩模式：仅显示内容与背景重叠的部分。
- 节目切换触发器：设置屏幕分区的动画重复指定次数后，跳转到的节目。可设置绝对位置、相对位置、目标范围，以及是否溯源。
- 继承节目：详见继承节目说明。

### 第二节 展示

#### 预览路牌的显示效果
点击“预览”，当前的路牌将会启动，并以当前节目为开始进行显示。

#### 打开路牌
在“显示屏”菜单中，各项目的含义如下：

![显示屏菜单](/doc_image/显示屏菜单.png)

- 打开所有显示屏：打开该线路的所有路牌；
- 关闭所有显示屏：关闭所有打开的路牌；
- 屏幕截图：为当前打开的所有路牌截图，并保存到软件安装目录的“Screenshots”文件夹；
- 置顶显示屏：切换路牌的置顶状态；
- 显示屏保持速度：在实际帧率不足的情况下，确保显示屏中的内容、动画按照预期速度展示。

点击“打开所有显示屏”，当前线路的所有路牌将会启动。

#### 打开路牌后可进行的操作
在显示屏窗口的右键菜单中，各项目的含义如下：

![显示屏右键菜单1](/doc_image/显示屏右键菜单1.png)
![显示屏右键菜单2](/doc_image/显示屏右键菜单2.png)

- 关闭窗口：关闭当前路牌。
- 窗口置顶：切换当前窗口的置顶状态。
- 屏幕截图：为当前路牌截图，并保存到软件安装目录的“Screenshots”文件夹。
- 开始录制GIF：直接开始在当前路牌录制GIF。
- 从节目开头开始录制GIF：重新开始播放当前节目，并在当前路牌录制GIF。
- 结束录制GIF：直接结束在当前路牌录制GIF。
- 节目完成后结束录制GIF：等待当前节目结束后自动结束录制GIF。
- 结束请耐心等待(^_^)：结束录制GIF后，稍等片刻GIF文件才能保存好。

开始录制GIF后，右键菜单会由图一样式变为图二样式。结束录制GIF后会变回图一样式。

**注意：不建议录制过长的GIF，因为可能会保存失败。**

打开路牌后，可以观看节目的自动切换，也可以在节目单中手动指定要播放的节目。

### 第三节 其他

#### 图标管理器

![图标管理器](/doc_image/图标管理器.png)

点击“加载图标”，可从现有的图标信息文件中加载图标。双击列表项，可在“节目内容编辑”的“文本”末尾插入图标。

#### 更多功能菜单

![更多功能菜单](/doc_image/更多功能菜单.png)

- 指定背景文件夹：为“屏幕设置”中的“背景设置”指定图片背景文件夹。将从这个文件夹中寻找背景图片。
- 管理图标信息文件：管理常用的图标信息文件。添加进列表的图标信息文件将在下次启动LED模拟器时自动加载。请注意定期清理失效的文件。
- 复制线路：复制线路列表中选定的线路。
- 粘贴线路：将上次复制的线路添加到线路列表中。
- 关于：打开“关于”界面。“关于”界面中的显示屏可用鼠标交互。
- 退出：退出程序，快捷键为Ctrl+Q。

#### 状态栏
显示程序运行中的一些信息，如路牌的帧率、是否正在录制GIF、文件保存提示、操作提示等信息。

## 附录

### 图标信息文件和图标
一个图标信息文件应遵循以下格式：
```text
ICON,{BMP},DIR:{图标所在路径},
{图标代号1},{图标文件名1},
{图标代号2},{图标文件名2},
{图标代号3},{图标文件名3},
......
```
其中，BMP是占位符，没有实际含义，**图标所在路径**可使用绝对路径和相对路径，相对路径从LED模拟器主程序出发，这个路径指向图标文件所在的文件夹。也可使用"default"占位，此时图标文件应和图标信息文件放置在同一文件夹中。路径不需要用引号包裹。
图标文件支持BMP、JPG、PNG等常见图片格式。

当图标文件为BMP格式时，显示模式为白底黑字。如果使用单色路牌，黑色部分为亮灯，白色部分为灭灯。如果使用彩色路牌，会为黑色部分着色为显示内容设定的颜色，黑色部分亮灯为所选颜色，白色部分为透明。
当图标为JPG或PNG格式时，在单色路牌中，会将图标颜色二值化后再反色显示，在彩色路牌中按照原样显示。

### 字体信息文件
字体信息文件应遵循以下格式，并会在程序启动时自动加载唯一的一个字体文件"/resources/font.info"：
```text
# 字体信息文件

# 位图ASCII字体
FONT,ASCII_FONT,DIR:./resources/bmpFont/,
{字体BMP图像文件名1},{BMP字体名称1},
{字体BMP图像文件名2},{BMP字体名称2},
......

# 16进制文本ASCII字体
FONT,ASCII_FONT,DIR:./resources/fontFont/,
{FONT字体文件名1},{FONT字体名称1},
{FONT字体文件名2},{FONT字体名称2},
......

# 携带的ttc、ttf字体
FONT,SYS_FONT,DIR:./resources/truetypeFont/,
{TTC/TTF字体文件名1},{字体名称1},
{TTC/TTF字体文件名2},{字体名称2},
......

# 安装的系统字体
FONT,SYS_FONT,DIR:C:/Windows/Fonts/,
{TTC/TTF字体文件名3},{字体名称3},
{TTC/TTF字体文件名4},{字体名称4},
......

# HZK汉字
FONT,HZK_FONT,DIR:./resources/binFont/,
{HZK字体文件名1},{HZK字体名称1},
{HZK字体文件名2},{HZK字体名称2},
......
```
如需添加字体，将字体放入对应文件夹，然后根据字体所属类别在各项下面按样添加即可。

### 添加自制点阵字体
#### 一、准备工具

Windows画图、文本编辑器、Photoshop（可选）

#### 二、BMP点阵ASCII字体简介

制作BMP点阵ASCII字体前，请确认需要制作的字体的单字大小。如高度为16，宽度为8

本教程制作的字体最终成果是图片形式。图片中的字符是按ASCII码排列的，每行16个，共16行(如无特殊需求，后8行可以留空)。字体命名有一定要求。以“ASCII_16-16_BB.bmp”为例，前面的ASCII表示这个是ASCII字符的字体，_16-16表示每个字符占据的空间是16\*16，_BB是后缀，表示是加粗了两次(后缀可以随便起，其他部分不能随便起)，整个图片的宽度和高度应该大于等于（字符宽度\*16，字符高度\*16）。

当每个字符占据的空间为正方形（宽度等于高度）时，程序将自动确定该字符的最小宽度，并将该字符按最小宽度显示（即去除字符两边的空白，字符为可变宽度），否则程序则会将该字符占据的所有空间全部作为该字符的字形（字符为固定宽度）


#### 三、绘制字体图片

本部分将以绘制一个高度为16的可变宽度字体为例。

##### （一）、Photoshop版

（1）、新建图片

宽度和高度均为256

![新建图片](/doc_image/新建图片.png)

（2）、绘制字体字形

为了方便确认字符的位置，先将Photoshop菜单栏中的“视图->显示->网格”勾选上，然后在Photoshop首选项中将网格线间隔设置为16

![网格设置](/doc_image/网格设置.png)

显示如图：

![界面显示1](/doc_image/界面显示1.png)

然后使用铅笔工具等工具绘制出需要的字符，绘制完成后效果如图所示：

![绘制完成1](/doc_image/绘制完成1.png)

将图片导出为PNG，然后用Windows画图工具打开，另存为单色位图，注意命名规范：

![画图保存1](/doc_image/画图保存1.png)

##### （二）、Windows画图版

（1）、根据需要制作的字体的单字大小，使用Windows画图打开附件中对应大小的网格纸，如打开"grid_paper_16-16.png"

（2）、根据参考图中的字符顺序，在相应位置绘制出需要的字形，如图所示：

![画图绘制1](/doc_image/画图绘制1.png)

（3）、绘制完全部需要的字形后，将文件另存为单色位图，注意命名规范，网格线自动消失，只留下字形。

#### 四、将字体图片导入LED模拟器配置文件

先将制作完成的字体的图片(要是单色位图)放在LED模拟器目录的\resources\bmpFont文件夹下，然后打开\resources文件夹中的font.info文件，在“FONT,ASCII_FONT,DIR:./resources/bmpFont/,”这一行下添加你的字体图片的名称和要称呼这个字体的名称，保存即可。如图所示：注意文件编码方式为UTF-8，不要是其他的类型（如ANSI或者带有BOM的UTF-8等）

![添加配置](/doc_image/添加配置.png)

#### 五、重新启动LED模拟器

重新启动LED模拟器后，即完成自制字体的导入。如果程序不能正常打开或闪退了，请检查上述步骤是否有误，尤其是文件命名、导入配置文件和保存后配置文件的编码方式几点。

### 常用的日期时间表示
```text
%y  两位数的年份表示（00-99）
%Y  四位数的年份表示（000-9999）
%m  月份（01-12）
%d  月内中的一天（0-31）
%H  24小时制小时数（0-23）
%I  12小时制小时数（01-12）
%M  分钟数（00=59）
%S  秒（00-59）
%a  本地简化星期名称
%A  本地完整星期名称
%b  本地简化的月份名称
%B  本地完整的月份名称
%c  本地相应的日期表示和时间表示
%j  年内的一天（001-366）
%p  本地A.M.或P.M.的等价符
%U  一年中的星期数（00-53）星期天为星期的开始
%w  星期（0-6），星期天为星期的开始
%W  一年中的星期数（00-53）星期一为星期的开始
%x  本地相应的日期表示
%X  本地相应的时间表示
%Z  当前时区的名称
%%  %号本身
%#X X代表上述显示内容有数字的指令：去掉占位的“0” 
```


